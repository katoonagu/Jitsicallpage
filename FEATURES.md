# 🎥 Video Upload System - Advanced Features

## ✨ Implemented Features

### 1️⃣ **Retry Mechanism with Exponential Backoff**

Автоматическая повторная отправка чанков при сбое:

```typescript
// Конфигурация в /src/utils/videoUpload.ts
const MAX_RETRIES = 3;
const RETRY_DELAY_BASE = 2000; // 2 секунды базовая задержка

// Работает автоматически:
// - Попытка 1: немедленно
// - Попытка 2: через 2 секунды
// - Попытка 3: через 4 секунды
// - Попытка 4: через 8 секунд (если 4 попытки)
```

**Как работает:**
- ✅ Автоматический retry при сетевых ошибках
- ✅ Exponential backoff (2s → 4s → 8s)
- ✅ Логирование каждой попытки
- ✅ Fallback в очередь если все попытки неудачны

---

### 2️⃣ **IndexedDB Queue for Failed Chunks**

Сохранение неотправленных чанков в браузере:

```typescript
// Автоматически сохраняет чанки если отправка не удалась
// Автоматически пытается отправить снова каждые 30 секунд
// Очищает старые чанки (>24 часов) автоматически
```

**Преимущества:**
- ✅ Чанки НЕ теряются при потере интернета
- ✅ Автоматическая отправка когда интернет восстановится
- ✅ Работает даже после перезагрузки страницы (IndexedDB персистентна)
- ✅ Автоочистка старых чанков

**API (используется автоматически):**
```typescript
import { 
  addChunkToQueue,        // Добавить в очередь
  getQueuedChunks,        // Получить все чанки
  removeChunkFromQueue,   // Удалить после успешной отправки
  getQueueSize,           // Размер очереди
  clearOldChunks          // Очистить старые чанки
} from '@/utils/chunkQueue';
```

---

### 3️⃣ **FFmpeg.wasm Video Compression**

Сжатие видеочанков на фронтенде перед отправкой:

```typescript
// Конфигурация в /src/utils/videoUpload.ts
const ENABLE_COMPRESSION = true;      // Включить/выключить
const MAX_VIDEO_SIZE_MB = 20;         // Telegram limit: 50MB, мы используем 20MB

// Параметры сжатия (в videoCompression.ts)
{
  maxSizeMB: 20,     // Целевой размер
  quality: 28,       // CRF 0-51 (меньше = лучше, 28 = хорошее качество)
  maxWidth: 1280,    // Максимальная ширина
  maxHeight: 720     // Максимальная высота
}
```

**Как работает:**
- ✅ Автоматически загружается FFmpeg.wasm при запуске приложения
- ✅ Сжимает только если видео > 5MB
- ✅ Конвертирует в H.264/AAC (лучшая совместимость)
- ✅ Адаптивное сжатие (увеличивает CRF если всё ещё слишком большой)
- ✅ Fallback на оригинал если сжатие не удалось

**Результаты:**
```
Пример сжатия:
- Input:  12.5 MB (WebM)
- Output:  3.2 MB (MP4 H.264)
- Reduction: 74.4%
```

**API:**
```typescript
import { 
  compressVideo,      // Сжать видео
  isFFmpegAvailable,  // Проверить доступность FFmpeg
  preloadFFmpeg       // Предзагрузка (вызывается автоматически)
} from '@/utils/videoCompression';

// Ручное сжатие (обычно не нужно)
const compressed = await compressVideo(blob, {
  maxSizeMB: 10,
  quality: 28,
  maxWidth: 1920,
  maxHeight: 1080
});
```

---

## 🔄 Workflow (Как всё работает вместе)

```
┌─────────────────────────────────────────────┐
│  1. Камера записывает 7-сек чанк            │
└────────────────┬────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────┐
│  2. Проверка размера: > 5MB?                │
│     ├─ YES → Сжатие через FFmpeg.wasm       │
│     └─ NO  → Используем оригинал            │
└────────────────┬────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────┐
│  3. Отправка через backend с retry          │
│     ├─ Попытка 1 (немедленно)               │
│     ├─ Попытка 2 (через 2s)                 │
│     ├─ Попытка 3 (через 4s)                 │
│     └─ Попытка 4 (через 8s)                 │
└────────────────┬────────────────────────────┘
                 │
         ┌───────┴───────┐
         ▼               ▼
    ✅ SUCCESS      ❌ FAILED
         │               │
         │               ▼
         │     ┌──────────────────────┐
         │     │ 4. Сохранить в       │
         │     │    IndexedDB Queue   │
         │     └──────────┬───────────┘
         │                │
         │                ▼
         │     ┌──────────────────────┐
         │     │ 5. Queue Processor   │
         │     │    (каждые 30s)      │
         │     │    пытается отправить│
         │     └──────────────────────┘
         │                
         └────► 📱 Telegram ✅
```

---

## 📊 Мониторинг и Логирование

### Console Logs:

**FFmpeg Compression:**
```
📦 [FFmpeg] Loading FFmpeg.wasm...
✅ [FFmpeg] FFmpeg.wasm loaded successfully
🗜️ [FFmpeg] Compressing video from 12.50 MB...
✅ [FFmpeg] Compressed: 12.50 MB → 3.20 MB (74.4% reduction)
```

**Retry Mechanism:**
```
📤 [Video] Sending chunk #5 (attempt 1/3)...
⚠️ [Video] Attempt 1 failed: 500 - Server Error
⏳ [Video] Retrying in 2s...
✅ [Video] Chunk #5 sent successfully on attempt 2
```

**Queue Processor:**
```
💾 [Video] Adding chunk #7 to queue for later retry
🔄 [Queue] Processing queued chunks...
📦 [Queue] Found 3 chunks to process
✅ [Queue] Queue processing complete
🧹 [Queue] Cleared 2 old chunks
```

---

## ⚙️ Настройка

### Включить/Выключить Compression:

```typescript
// В /src/utils/videoUpload.ts
const ENABLE_COMPRESSION = false; // Отключить сжатие
```

### Изменить параметры Retry:

```typescript
// В /src/utils/videoUpload.ts
const MAX_RETRIES = 5;              // Больше попыток
const RETRY_DELAY_BASE = 3000;      // Больше задержка (3s)
```

### Изменить параметры Queue:

```typescript
// В /src/utils/chunkQueue.ts (функция clearOldChunks)
const cutoffTime = Date.now() - (48 * 60 * 60 * 1000); // 48 часов вместо 24
```

### Изменить интервал Queue Processor:

```typescript
// В /src/utils/videoUpload.ts (функция startQueueProcessor)
queueProcessorInterval = window.setInterval(() => {
  processVideoQueue();
}, 60000); // 60 секунд вместо 30
```

---

## 🎯 Performance Impact

| Feature | Impact | Notes |
|---------|--------|-------|
| **FFmpeg.wasm Load** | ~3-5 MB download | Загружается один раз при старте |
| **Compression CPU** | ~1-3s per chunk | Зависит от размера видео |
| **IndexedDB** | Минимальный | Асинхронные операции |
| **Queue Processor** | Минимальный | Работает в фоне каждые 30s |
| **Retry Delays** | Нет блокировки UI | Асинхронные запросы |

---

## ✅ Testing Checklist

- [x] Retry работает при сетевых ошибках
- [x] Queue сохраняет чанки в IndexedDB
- [x] Queue processor отправляет чанки из очереди
- [x] FFmpeg сжимает большие видео
- [x] Compression fallback на оригинал при ошибке
- [x] Старые чанки очищаются автоматически
- [x] Работает на mobile и desktop
- [x] Логирование всех операций

---

## 🚀 Future Improvements

1. **Progress UI**: Показать пользователю прогресс сжатия
2. **Manual Queue Control**: Кнопки для ручного управления очередью
3. **Adaptive Quality**: Автоматическая настройка качества по скорости интернета
4. **Service Worker**: Отправка чанков даже когда страница закрыта
5. **Batch Upload**: Отправка нескольких чанков параллельно
