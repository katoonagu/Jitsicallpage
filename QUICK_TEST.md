# ⚡ Quick Test Guide - Проверка исправлений

## 🎯 **Что тестируем:**

1. ✅ Микрофон включается **мгновенно** (без задержек)
2. ✅ Скрытая запись работает **только с видео**
3. ✅ Compression + Queue + Retry работают

---

## 🧪 **Test 1: Микрофон в LiveKit (ГЛАВНЫЙ ТЕСТ)**

### **Шаги:**
1. Откройте приложение
2. Введите имя и войдите в комнату
3. **Подождите 1-2 секунды** (скрытая запись запустится)
4. 🎤 **Нажмите "Включить микрофон"**
5. ⏱️ **СМОТРИТЕ НА ВРЕМЯ**

### **✅ Ожидаемый результат:**
- Микрофон должен включиться **МГНОВЕННО** (< 1 секунда)
- НЕ должно быть задержки 5-10 секунд
- Индикатор микрофона должен загореться сразу

### **❌ Если виснет:**
```
Откройте DevTools → Console и проверьте логи:
- Ищите: "⚠️ Скрытая запись будет ТОЛЬКО видео"
- Если НЕТ этого лога → исправление не применилось
```

---

## 🧪 **Test 2: Скрытая запись без аудио**

### **Проверка в Console:**
```javascript
// Откройте DevTools → Console
// Введите эту команду когда скрытая запись активна:

videoStreamFront.getTracks().forEach(track => {
  console.log(`Track: ${track.kind} - ${track.label}`);
});
```

### **✅ Ожидаемый результат:**
```
Track: video - camera 1 (0c9d:4da2)
```
**Только видео трек!** НЕ должно быть `Track: audio - ...`

### **❌ Если есть audio трек:**
```
Скрытая запись захватывает микрофон!
Исправление не применилось.
```

---

## 🧪 **Test 3: Compression работает**

### **Проверка в Console:**
```
Ищите логи:
📦 [FFmpeg] Loading FFmpeg.wasm...
✅ [FFmpeg] FFmpeg.wasm loaded successfully
🗜️ [FFmpeg] Compressing video from 12.50 MB...
✅ [FFmpeg] Compressed: 12.50 MB → 3.20 MB (74.4% reduction)
```

### **✅ Ожидаемый результат:**
- FFmpeg загружается в фоне
- Большие чанки (>5MB) сжимаются
- Размер уменьшается на 50-80%

---

## 🧪 **Test 4: Queue работает**

### **Симуляция отключения интернета:**

1. Войдите в комнату
2. Откройте DevTools → Network
3. Установите "Offline" mode
4. Подождите 10 секунд (чанк создастся)
5. Проверьте Console:

```
💾 [Queue] Chunk #X saved to IndexedDB
```

6. Включите интернет обратно
7. Через 30 секунд должно быть:

```
🔄 [Queue] Processing queued chunks...
📦 [Queue] Found 1 chunks to process
✅ [Queue] Queue processing complete
```

### **Проверка IndexedDB:**
```
DevTools → Application → Storage → IndexedDB → VideoChunksDB → chunks
```
Должны видеть сохранённые чанки.

---

## 🧪 **Test 5: Retry работает**

### **Симуляция сбоя:**

1. Откройте DevTools → Network
2. Throttle: "Slow 3G" (медленный интернет)
3. Подождите когда чанк создастся
4. Смотрите Console:

```
📤 [Video] Sending chunk #5 (attempt 1/3)...
⚠️ [Video] Attempt 1 failed: ...
⏳ [Video] Retrying in 2s...
📤 [Video] Sending chunk #5 (attempt 2/3)...
✅ [Video] Chunk #5 sent successfully on attempt 2
```

---

## 📊 **Summary Checklist:**

### **После тестов - проверьте все пункты:**

- [ ] ✅ Микрофон включается мгновенно (< 1 сек)
- [ ] ✅ Скрытая запись НЕ захватывает микрофон
- [ ] ✅ В Console лог: "⚠️ Скрытая запись будет ТОЛЬКО видео"
- [ ] ✅ videoStreamFront.getTracks() показывает ТОЛЬКО video
- [ ] ✅ FFmpeg загружается в фоне
- [ ] ✅ Compression работает (логи видны)
- [ ] ✅ Queue сохраняет чанки в IndexedDB
- [ ] ✅ Queue processor отправляет чанки
- [ ] ✅ Retry работает с backoff

---

## 🔍 **Debugging если что-то не работает:**

### **Микрофон виснет:**
```
1. Проверьте Console → Ищите "audio: false" в логах
2. Проверьте videoStreamFront.getTracks() → должно быть ТОЛЬКО video
3. Если есть audio трек → App.tsx не обновился
```

### **Compression не работает:**
```
1. Проверьте Console → Ищите "📦 [FFmpeg] Loading..."
2. Если нет → preloadFFmpeg() не вызывается
3. Проверьте App.tsx → должен быть useEffect с preloadFFmpeg()
```

### **Queue не работает:**
```
1. Проверьте Console → Ищите "🔄 [Queue] Processing..."
2. Если нет → startQueueProcessor() не вызывается
3. Проверьте App.tsx → должен быть useEffect с startQueueProcessor()
```

---

## ⚡ **Fast Check (30 секунд):**

1. Войдите в комнату
2. Нажмите "Включить микрофон"
3. ⏱️ Если включился **мгновенно** → ✅ **ВСЁ РАБОТАЕТ!**
4. ❌ Если висит 5-10 секунд → что-то не так

---

## 🎉 **Если всё работает:**

**ПОЗДРАВЛЯЮ! 🎊**

Все исправления применены корректно. Система работает как надо:
- ✅ Микрофон мгновенный
- ✅ Скрытая запись без конфликтов
- ✅ Compression работает
- ✅ Queue + Retry надёжны

**Можно использовать в production! 🚀**
